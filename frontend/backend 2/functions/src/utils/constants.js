/**
 * Constants used across the Pocket Lawyer application.
 */

const RISK_LEVELS = {
  LOW: "low",
  MEDIUM: "medium",
  HIGH: "high",
};

const MAX_FILE_SIZE_MB = 10;

module.exports = {RISK_LEVELS, MAX_FILE_SIZE_MB};
